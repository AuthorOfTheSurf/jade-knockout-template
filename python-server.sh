#!/bin/bash

# Start a python server to serve up the html locally. Without this you would
# have to double-click the html files. This is closer to what the development
# environment would be like. Requires python 2.7
# I recommend running this in its own terminal window/tab
python -m SimpleHTTPServer 8000
